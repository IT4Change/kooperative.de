services:
  mariadb:
    build: ./database
    container_name: koop-mariadb
    restart: unless-stopped
    ports:
      - "3306:3306"
    volumes:
      - db-data:/var/lib/mysql

volumes:
  db-data:
