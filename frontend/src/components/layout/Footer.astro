---
const currentYear = new Date().getFullYear();

const mainLinks = [
  { label: 'Arbeit', href: '/arbeit' },
  { label: 'Kultur', href: '/kultur' },
  { label: 'Bildung', href: '/bildung' },
  { label: 'Gäste', href: '/gaeste' },
  { label: 'Bestellung', href: '/bestellung' },
  { label: 'Kalender', href: '/kalender' },
];

const infoLinks = [
  { label: 'Historie', href: '/historie' },
  { label: 'Kontakt', href: '/kontakt' },
  { label: 'Impressum', href: '/impressum' },
  { label: 'Datenschutz', href: '/datenschutz' },
];
---

<footer class="bg-primary-900 text-primary-100 mt-auto">
  <div class="container-page py-12 md:py-16">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-10 md:gap-12">
      <!-- About Column -->
      <div>
        <h3 class="text-white text-lg font-bold mb-4">Kooperative Dürnau</h3>
        <p class="text-primary-200 text-sm leading-relaxed">
          Die Kooperative Dürnau ist eine Gemeinschaft von Menschen, die zusammen leben, arbeiten
          und gestalten. Auf dem Gelände der ehemaligen Hofstelle verbinden wir nachhaltige
          Landwirtschaft, Handwerk, Kultur und Bildung.
        </p>
      </div>

      <!-- Navigation Column -->
      <div>
        <h3 class="text-white text-lg font-bold mb-4">Navigation</h3>
        <ul class="space-y-2">
          {mainLinks.map((link) => (
            <li>
              <a
                href={link.href}
                class="text-primary-200 hover:text-white transition-colors text-sm no-underline"
              >
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Contact Column -->
      <div>
        <h3 class="text-white text-lg font-bold mb-4">Kontakt</h3>
        <address class="not-italic text-primary-200 text-sm leading-relaxed space-y-2">
          <p>
            Kooperative Dürnau e.V.<br />
            Kirchstraße 1<br />
            73105 Dürnau
          </p>
          <p>
            <a
              href="mailto:info@kooperative.de"
              class="text-primary-200 hover:text-white transition-colors no-underline"
            >
              info@kooperative.de
            </a>
          </p>
        </address>

        <div class="mt-6">
          <h4 class="text-white text-sm font-semibold mb-2">Rechtliches</h4>
          <ul class="space-y-1">
            {infoLinks.map((link) => (
              <li>
                <a
                  href={link.href}
                  class="text-primary-300 hover:text-white transition-colors text-xs no-underline"
                >
                  {link.label}
                </a>
              </li>
            ))}
          </ul>
        </div>
      </div>
    </div>

    <!-- Bottom Bar -->
    <div class="mt-10 pt-6 border-t border-primary-800">
      <p class="text-primary-400 text-xs text-center">
        &copy; {currentYear} Kooperative Dürnau e.V. Alle Rechte vorbehalten.
      </p>
    </div>
  </div>
</footer>
